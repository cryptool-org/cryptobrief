Unit tests included, but rely on JUnit library that *is not included*, 
so for purposes of testing, just extracts the zip file into this folder, link the JUnit library to the project,
add your own tests, and compile